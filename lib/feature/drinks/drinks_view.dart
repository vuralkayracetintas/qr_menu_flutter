import 'package:flutter/material.dart';

class DrinksView extends StatelessWidget {
  const DrinksView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('DrinksView'),
      ),
    );
  }
}
